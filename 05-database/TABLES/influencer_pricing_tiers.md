# influencer_pricing_tiers

**Description**: Package pricing per influencer

**TypeScript type**: `InfluencerPricingTier` (defined in `types/database.ts`)

**Key columns**: `id,influencer_id,tier_name,price`

**Used by**:
- See `FORM_TO_DATABASE_MAPPING.md` for all form writes to this table
- See `lib/queries.ts` for all read queries

**RLS**: See `DATABASE_SCHEMA.md` — section for this table
