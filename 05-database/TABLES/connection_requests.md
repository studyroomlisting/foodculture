# connection_requests

**Description**: Restaurants reaching out to influencers

**TypeScript type**: `ConnectionRequest` (defined in `types/database.ts`)

**Key columns**: `id,influencer_id,restaurant_name,status`

**Used by**:
- See `FORM_TO_DATABASE_MAPPING.md` for all form writes to this table
- See `lib/queries.ts` for all read queries

**RLS**: See `DATABASE_SCHEMA.md` — section for this table
