# activity_feed

**Description**: Live activity stream for homepage

**TypeScript type**: `ActivityFeedItem` (defined in `types/database.ts`)

**Key columns**: `id,message,dot_color,created_at`

**Used by**:
- See `FORM_TO_DATABASE_MAPPING.md` for all form writes to this table
- See `lib/queries.ts` for all read queries

**RLS**: See `DATABASE_SCHEMA.md` — section for this table
