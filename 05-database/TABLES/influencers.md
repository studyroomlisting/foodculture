# influencers

**Description**: Food creators ranked by visit impact

**TypeScript type**: `Influencer` (defined in `types/database.ts`)

**Key columns**: `id,slug,name,handle,impact_score,connection_fee`

**Used by**:
- See `FORM_TO_DATABASE_MAPPING.md` for all form writes to this table
- See `lib/queries.ts` for all read queries

**RLS**: See `DATABASE_SCHEMA.md` — section for this table
