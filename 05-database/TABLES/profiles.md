# profiles

**Description**: User profiles auto-created on signup

**TypeScript type**: `Profile` (defined in `types/database.ts`)

**Key columns**: `id,full_name,role,onboarding_complete`

**Used by**:
- See `FORM_TO_DATABASE_MAPPING.md` for all form writes to this table
- See `lib/queries.ts` for all read queries

**RLS**: See `DATABASE_SCHEMA.md` — section for this table
