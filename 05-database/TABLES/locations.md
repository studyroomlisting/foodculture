# locations

**Description**: Sub-area neighbourhood pages

**TypeScript type**: `Location` (defined in `types/database.ts`)

**Key columns**: `id,zone_id,name,slug,restaurant_count`

**Used by**:
- See `FORM_TO_DATABASE_MAPPING.md` for all form writes to this table
- See `lib/queries.ts` for all read queries

**RLS**: See `DATABASE_SCHEMA.md` — section for this table
