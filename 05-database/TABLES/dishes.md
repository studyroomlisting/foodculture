# dishes

**Description**: Trending dishes across Bengaluru

**TypeScript type**: `Dish` (defined in `types/database.ts`)

**Key columns**: `id,name,emoji,trend_label,restaurant_count`

**Used by**:
- See `FORM_TO_DATABASE_MAPPING.md` for all form writes to this table
- See `lib/queries.ts` for all read queries

**RLS**: See `DATABASE_SCHEMA.md` — section for this table
