# listing_claims

**Description**: Ownership claim requests

**TypeScript type**: `ListingClaim` (defined in `types/database.ts`)

**Key columns**: `id,restaurant_id,claimant_id,status`

**Used by**:
- See `FORM_TO_DATABASE_MAPPING.md` for all form writes to this table
- See `lib/queries.ts` for all read queries

**RLS**: See `DATABASE_SCHEMA.md` — section for this table
